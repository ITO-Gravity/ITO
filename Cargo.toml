[package]
name = "ito"
version = "0.1.0"
edition = "2021"
authors = ["Vasloth Team"]
description = "Semantic version control engine for hardware engineering (BOM, CAD, and Firmware)"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
